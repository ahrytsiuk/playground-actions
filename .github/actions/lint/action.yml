name: "Lint Action"
description: "Runs linting checks on the codebase."

runs:
  using: "composite"
  steps:
    - name: Simple action
      uses: actions/github-script@v8
      with:
        script: |
          const { main: script } = await import('${{ github.action_path }}/src/index.ts');
          return await script();
